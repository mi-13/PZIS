"""
Лаб 4.4
Дан список 3 слов различной длины разделенных пробелами. Составить программу упорядочения списка слов: 

варіант f
в порядке возрастания количества гласных в слове;
"""

chars = ['а','е','и','і','о','у']

text = input("Enter text with ' ' as separator")
words = text.split(' ')
rezult = {}

counter = 0
for i in range(len(words)):
  counter = 0
  word = words[i]
  print(word)
  for j in range(len(word)):
    if (word[j] in chars):
      counter = counter + 1
  rezult[words[i]] = counter

ordered_list = list()
for k,v in sorted(rezult.items(),key = lambda x:(x[1],x[0]),):
  print(str(v) + " - " + str(k))
